import React, { Component, Fragment } from 'react';

export default class AddProposal extends Component{
    render(){
        return(
            <Fragment>
                Add Proposals
            </Fragment>
        )
    }
}